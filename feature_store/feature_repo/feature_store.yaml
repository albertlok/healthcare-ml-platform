project: healthcare_ml
registry: data/feast_registry.db
provider: local

offline_store:
  type: file                            # Reads parquet files; swap to duckdb/snowflake for prod

online_store:
  type: sqlite                          # Free local dev; swap to redis or dynamodb for prod
  path: data/online_store.db

entity_key_serialization_version: 3
